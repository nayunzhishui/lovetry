# 项目执行记录

> 对应计划：`情侣小程序开发计划.md`
> 项目：`D:\codex\workspace\lovetry`
> 执行日期：2026-07-15
> 最近更新：2026-07-18
> 当前状态：P0-P10 本地自动化开发完成；真实环境、开发者工具视觉编译与真机验收冻结

## 1. 阶段总览

| 阶段 | 本地实现 | 自动验证 | 外部人工验证 |
|---|---|---|---|
| P0 工程与安全地基 | 自动完成 | 通过 | 待真实 AppID、云环境、三账号和真机 |
| P1 情侣核心闭环 | 自动完成 | 通过 | 待双账号并发与越权实测 |
| P2 共同生活模块 | 自动完成 | 通过 | 待开发者工具和真机交互实测 |
| P3 相册与媒体 | 自动完成 | 通过 | 待存储规则、上传费用和第三账号实测 |
| P4 聚合、备份与发布 | 自动完成 | 通过 | 待部署、真实云数据恢复、体验版 7 天和提审 |
| P5 GitHub 对标增强与 UI 美化 | 自动完成 | 通过 | 待开发者工具、订阅发送、真实环境与真机 |
| P6 全栈质量冲刺 | 自动完成 | 通过 | 待真实环境、双/三账号、弱网和真机 |
| P7 恋爱智能 Agent 与 API | 自动完成 | 通过 | 待真实模型、CloudBase、费用和专业复核 |
| P8 GitHub 对标录入体验 | 自动完成 | 通过 | 待草稿恢复、长文本、小屏和真机实测 |
| P9 今日行动与清单层级 | 自动完成 | 通过 | 待滚动、触控、屏幕阅读器和真机实测 |
| P10 多专业综合审查与修正 | 自动完成 | 通过 | 待真实模型、专业复核、双人访谈和真机 |

“自动完成”只表示工作区代码、文档和本地自动检查完成，不代表 CloudBase 或微信审核已经完成。

## 2. 已完成实现

### P0：工程、安全与可联调基础

- 建立唯一开发计划和本执行记录；
- 新增 Node 测试、共享常量、权限、记录、时长、奖励幂等纯业务模块；
- 新增 `services/cloudApi.js`，页面不再自行重复调用云函数；
- 加入码升级为安全 8 位随机码，包含唯一检查、7 天过期和事务加入；
- 新增 `memberships` 唯一归属文档，限制账号只能属于一个活跃空间；
- 解除关系改为事务归档整个空间，邀请码立即失效，历史空间不会开放给新成员；
- records 支持权限查询、详情、编辑、事务版本检查、软删除、筛选和统计；
- 联调页覆盖 login/couple/records/plans/rewards/media/dashboard；
- 统一奶油纸张、琥珀棕、鼠尾草绿及“双轨时间线”视觉系统。

### P1：情侣核心闭环

- 首页：在一起天数、今日安排、待办、最近心情、积分和近 7 天专注；
- 记录：生活日记、心情、沟通复盘的新增/列表/详情/编辑/删除与可见性；
- 计划：任务、事件、负责人、清单、状态、编辑、删除和日历聚合；
- 奖励：双钱包、获得/消费流水、余额校验、伴侣确认、持久幂等键和参数冲突校验；
- 日历：月视图、日期详情、类型筛选、纪念日循环事件和结果跳转。

### P2：共同生活模块

- 菜单：分类、标签、喜好、随机选择、避免立即重复、吃过后生成玩乐记录；
- 旅行：日期、预算、行程节点、准备清单、完成状态及玩乐记录关联；
- 玩乐：吃饭/酒店/旅行/约会、地点、金额、评价、分类和日期筛选；
- 睡眠、生理期、游戏：日期范围、跨午夜时长、默认隐私和 7/30 天统计；
- 番茄钟：专注/休息、暂停/恢复/中断/完成、关联任务、后台时间校准；
- 番茄记录以确定性请求 ID 保存；失败待保存状态持久化，重试不重复写入。
- 心情、吵架、睡眠、生理期和番茄记录的默认私密规则已统一，并有权限与幂等测试覆盖。

### P3：相册与媒体

- 相册 CRUD、封面、照片网格、预览、分页、上传进度和删除；
- 最多 6 张、单张 10MB、上传前压缩；
- 路径包含 coupleId/openid/albumId/随机名；服务端二次校验 fileID 与路径；
- 数据库仅保存 fileID，展示时获取临时 URL；
- 元数据写入失败会删除已上传文件；云文件删除失败标记 `pendingDeletion`。

### P4：聚合、搜索、备份与交接

- dashboard 提供首页、日历、搜索、导出、恢复和健康检查；
- 私密数据在数据库查询阶段过滤；
- 搜索支持关键词、来源、类型、日期和结果跳转；
- 备份包含版本、导出时间、截断标记；恢复校验空间 ID 并按 `restoredFromId` 去重；
- 只读云函数请求支持 12 秒超时和一次瞬时故障重试，写请求不在客户端自动重放；
- 监听网络断开与恢复并给出明确提示；
- 自动恢复演练拒绝跨空间、错误版本和截断备份，并限制单次恢复数量；
- 新增 `CLOUDBASE_SETUP.md` 和 `PRIVACY.md`；
- 云函数依赖固定为 `wx-server-sdk@4.0.2` 并生成锁文件。

## 3. 自动验证证据

| 检查 | 结果 |
|---|---|
| `npm test` | 21/21 通过 |
| 全部 JavaScript `node --check` | 42 个文件通过 |
| 全部 JSON `JSON.parse` | 33 个文件通过 |
| 页面结构 | 15 个已注册页面四件套完整 |
| 云函数结构 | 7 个云函数均含入口、依赖文件和锁文件 |
| 页面直接云函数调用 | 仅统一 `cloudApi.js` 内存在 |
| `git diff --check` | 通过，仅有 Git 行尾提示 |
| 独立代码审查 | 已执行；解绑泄露、上传规则、事务版本、奖励指派与幂等、私密分页等阻断项已修复 |

## 4. 微信开发者工具尝试

- 已检测到微信开发者工具 `2.01.2510290`；
- 已通过 CLI 开启工具服务能力并尝试导入项目；
- 本轮桌面控制运行时未加载可调用入口，官方 CLI 等待 IDE 端口超时；项目仍为 `touristappid`、云环境 ID 为占位值；
- 因此没有把“开发者工具编译通过”或“云函数部署成功”写成完成。

## 5. 外部人工验收清单

1. 在 `project.config.json` 配置真实 AppID，在 `miniprogram/config.js` 配置生产 CloudBase 环境 ID；
2. 按 `CLOUDBASE_SETUP.md` 创建 8 个集合、索引和云存储细粒度规则；
3. 部署 7 个云函数并运行联调检查页；
4. 账号 A/B 创建、加入、同步、私密越权、任务奖励和相册全链路；
5. 账号 C 验证无法加入、读取、上传、预览或删除；
6. 验证并发加入、重复奖励、重复恢复和弱网超时；
7. iOS/Android 真机检查安全区、日期、后台计时和图片选择；
8. 体验版双账号连续使用 7 天，完成隐私配置与提审材料。

## 6. 已知限制

- 本地无法代替真实 CloudBase 事务、索引和安全规则验证；
- 单次搜索最多返回 100 项；备份超过 500 条记录/计划、100 个相册或 500 张图片时会明确标记 `truncated` 并拒绝恢复，需扩充分批导出后再生成完整备份；
- 恢复首版只恢复记录和计划，不恢复钱包流水或云端图片文件，避免伪造余额和失效文件；
- 备份恢复逐项执行，异常时可能部分成功；重复执行会跳过已恢复来源 ID；
- 数据库索引、存储规则、用量告警和彻底数据注销必须在 CloudBase 控制台完成；
- 自动测试覆盖纯业务规则，真实双账号、第三账号和真机证据仍待人工补充。

## 7. Git 提交记录

- `3f42411 feat(miniprogram): 完成情侣生活管理核心闭环`：99 个文件，包含业务实现、云函数、测试、部署和隐私文档；
- `827354e docs(project): 记录开发计划与执行结果`：建立计划和执行证据基线；
- `967136d feat(reliability): 增加只读重试与备份校验`：补齐超时、重试、离线提示和自动恢复演练；
- `26fe945 fix(records): 对齐敏感记录隐私与幂等校验`：统一默认私密规则并验证番茄请求 ID。

## 8. P5 执行记录（2026-07-15）

- 已冻结真实 AppID、CloudBase 部署、订阅模板、开发者工具和真机验收；
- 已完成 GitHub UI/产品模式研究，证据见 `design/research.md`；
- 已比较三种美术方向并选定“Twin Almanac 双人年鉴”，规范见 `design/directions.md`；
- 自动开发顺序：同步 → 提醒 → 奖励商城 → 纪念日 → 备份 → 动态 → 组件/UI → CI/可观测性；
- 已完成 P5-02：增量同步支持分页合并、前台恢复、活动页 30 秒同步、离线和远端更新反馈；
- 已完成 P5-03：提醒偏好、任务/纪念日/奖励确认提醒、提醒存档和订阅消息占位配置；
- 已完成 P5-04：奖励商城、仓库、事务扣分、稳定幂等键和双人状态推进；
- 已完成 P5-05：多纪念日、按年重复、首页倒计时、日历实例和闰日回退；
- 已完成 P5-06：备份清单 v2、摘要校验、本地文件、分享和选择导入，服务端继续兼容 v1；
- 已完成 P5-07：共享动态、看见/抱抱/加油轻回应、私密过滤和请求冲突校验；
- 已完成 P5-08：四个公共组件、`features` 分包、17 页面路径与组件结构检查；
- 已完成 P5-09 编码：本地 verify、GitHub Actions、手动预览模板、追踪 ID 和脱敏结构化日志；
- UI 已落地 Twin Almanac：冷纸张底色、钴蓝/珊瑚双轨、苔绿状态、黄铜奖励/纪念日、宋体标题、四入口 TabBar，并覆盖小屏、深色和减少动效；
- 当前自动验收：37 项 Node 测试通过；17 个页面、8 个云函数、65 个 JS 文件通过结构、语法和 JSON 检查；
- 未完成：电脑控制执行入口在当前会话不可用，未取得微信开发者工具视觉编译证据；真实环境部署、订阅模板发送、双账号/三账号和真机验收继续冻结。
- `8a54637 feat(miniprogram): 完成 P5 关系互动与年鉴 UI`：同步、提醒、商城、纪念日、备份、动态、组件和 37 项测试；
- `c782f4f ci(project): 增加自动验证与预览模板`：本地 verify、GitHub Actions 与 Secrets 驱动的手动预览模板。

## 9. P6 一小时全栈质量冲刺（2026-07-16）

### 已完成

- 提醒偏好更新不再清除订阅模板和授权时间；
- 记录创建保存请求摘要，重复标识内容不一致时明确拒绝；
- 计划状态、清单和删除改为事务与版本并发控制；
- 情侣资料、邀请码返回值包含最新版本和过期时间；
- 同步改为记录/计划/提醒独立分页，并在客户端按文档 ID 去重；
- 搜索类型全部改为中文产品语言，加入类型选择、日期校验、清除筛选和结果日期；
- 首页、日历、记录、相册、搜索、设置的高频点击区改为原生按钮；
- 设置页增加默认私密说明，联调入口生产默认隐藏；
- 番茄钟界面刷新降至每秒一次，计时结果仍使用绝对时间；
- 新增 `npm run quality`、主包预算和非语义点击区自动门禁；
- README 与 CloudBase 验收说明已补齐。

### 自动验证证据

| 检查 | 结果 |
|---|---|
| `npm run quality` | 通过 |
| Node 测试 | 46/46 通过 |
| 页面结构 | 17 个页面四件套完整 |
| 云函数结构 | 8 个云函数入口、依赖和锁文件完整 |
| JavaScript | 73 个文件语法通过 |
| 主包源码 | 220.4 KiB，低于内部 1 MiB 预算 |
| 服务边界 | 页面无直接 `wx.cloud.callFunction` |
| 交互语义 | 普通页面无 `view/text + bindtap` |
| `git diff --check` | 通过，仅存在 Windows 行尾提示 |

### 本轮仍未宣称完成

- 未配置或部署真实 AppID、CloudBase、索引和存储规则；
- 未配置订阅消息真实模板；
- 未取得微信开发者工具编译截图；
- 未执行双账号、第三账号、iOS/Android 真机和体验版验收；
- 上述事项继续按用户要求冻结，不影响本地自动化开发结论。

## 10. P7 恋爱智能 Agent 执行记录（2026-07-16）

- 新增 `love-agent` 云函数和 12 条本地恋爱知识条目；
- 使用确定性中文检索选取最多 4 条知识，再向模型提供受限上下文；
- OpenAI Responses API 通过原生 HTTPS 调用，默认模型 `gpt-5.6-luna`，可由环境变量替换；
- 未配置密钥或模型调用失败时仍返回本地知识库答案；
- 模型回答仅允许引用本次检索条目，未知引用会被删除；
- 高风险内容分为即时危险、自伤和强制控制，优先安全响应或拒绝操控建议；
- Agent 明示 AI 身份、默认无记忆、不读取情侣记录、不保存问答正文；
- 新增首页入口和分包聊天页，支持建议问题、知识来源、复制和清空；
- Agent 无需先建立情侣空间；
- 每个 OpenID 每天最多 50 次真实模型请求；
- 新增 6 项 Agent 行为测试；当前全量自动测试为 52 项。
- 依赖审计确认 `wx-server-sdk@4.0.2` 已是 npm 当前最新版，但其传递依赖仍有 audit 告警；自动建议会降级主版本，因此未做高风险强制修复，已写入部署说明持续跟踪。

### P7 外部待办

- 在真实 CloudBase 创建 `agent_usage` 集合；
- 为 `love-agent` 设置 `OPENAI_API_KEY`，按需设置 `LOVE_AGENT_MODEL` 和 `LOVE_AGENT_API_BASE`；
- 部署云函数并验证目标网络可访问 Responses API；
- 监控费用、延迟、拒答率和用户反馈；
- 在正式发布前对知识库进行专业内容复核；
- 在隐私指引中披露外部模型处理范围。

## 11. P7-06 通用 API 接入（2026-07-17）

- 模型适配层现同时支持 OpenAI Responses 与 OpenAI 兼容 Chat Completions；
- 新增 `LOVE_AGENT_API_KEY` 通用密钥，同时向后兼容 `OPENAI_API_KEY`；
- API Base、相对路径、协议、模型、超时和输出上限均由云函数环境变量控制；
- Chat Completions 可按服务切换 `max_tokens` 或 `max_completion_tokens`；
- 生产默认只允许 HTTPS，本地 HTTP 必须显式开启；
- 恋爱助手页面新增脱敏模型状态和“测试 API”入口；
- 连接测试区分认证失败、地址不匹配、限流、超时和不可用，不向前端返回密钥、完整路径或供应商原始错误；
- API 调用失败会显示降级说明并继续返回本地知识库回答；
- 新增 4 项适配层行为测试（含本地模拟 API 端到端调用）；当前全量自动测试 56 项，真实密钥配置与 CloudBase 部署继续冻结。

## 12. P8 GitHub 对标录入体验优化（2026-07-18）

- 参考 Rainbow Cats 的快速预设、Love Diary 的原生本地缓存、Our Nest 的情侣内容组织、TDesign 的状态规范和 Super Productivity 的任务关联模式；
- 新增统一本机草稿深模块，记录和计划页面只调用保存、恢复、清理接口；
- 生活、心情、沟通、玩乐、睡眠、生理期、游戏、任务、事件、菜单、旅行和纪念日均有独立草稿空间；
- 草稿保存状态可见、7 天自动失效、成功写入云端后清除，可手动移除本机备份；
- 为 7 类记录和 5 类计划增加可编辑模板，降低空白表单压力；
- 首页快速经历同样支持草稿恢复；
- 日历增加今天标记、回到今天、空日期直接创建和日期预填；
- 奖励表单增加 5、10、20、50 积分快捷档位；
- 自动验证：59 项 Node 测试通过；18 个页面、9 个云函数、84 个 JavaScript 文件通过结构与语法检查；主包源码 238.0 KiB；
- 真实开发者工具与真机恢复体验继续保留为外部验收项。

## 13. P9 今日行动与清单层级优化（2026-07-18）

- 完成首页、共同计划和共同日历代码审计，三个高严重度问题均已修复并记录在 `design/audit.md`；
- 首页统计卡现可直达任务、日历和积分，新增心情、今日安排与沟通复盘一步入口；
- 共同计划改为清单优先，普通进入收起表单，日历带日期、草稿恢复和编辑时自动展开；
- 编辑计划会定位到表单，完成后继续恢复此前未提交的新计划草稿；
- 新增日历展示语义深模块，统一处理排序、中文类型、来源、状态、记录时间和计划全天语义；
- 日历选中任意日期均可继续添加，错误重试保留当前月份，切月自动选择目标月中的相近日；
- 视觉保持 Twin Almanac，仅用短双轨行动标记强化“自己 / 两人 / 沟通”含义；
- 自动验证：63 项 Node 测试通过；18 个页面、9 个云函数、86 个 JavaScript 文件通过结构与语法检查；主包源码 245.2 KiB；
- 微信开发者工具、真机渲染、屏幕阅读器与真实触控验证继续作为外部待办。

## 14. P10 多专业综合审查与修正（2026-07-18）

- 从产品体验、关系心理安全、工程实现、UI 美术与无障碍四个维度完成审查，结论与优先级见 `design/professional-review.md`；
- 恋爱助手不再向普通用户暴露模型、协议和主机等实现细节，每条回答固定展示 AI、知识库或安全模式；
- 成功回退本地知识库改为中性通知，并增加“梳理当前问题”“准备一次对话”两个可编辑输入框架；
- 模型提示新增事实、用户理解和未知信息的区分，禁止仅凭单方叙述确认伴侣动机；
- 风险识别补充窒息和性强迫表达，积分与奖励入口补充双方自愿、可拒绝及不可交换隐私、身体接触、道歉或控制权的边界；
- 深色主题为钴蓝、珊瑚、苔绿和黄铜建立独立高对比色，并用自动测试保证关键文字对比度至少 4.5:1；
- 确认弹窗补充对话框语义；移除无入口的旧沟通页面；移除微信全局配置中无效的 `requiredPrivateInfos.chooseMedia`；
- 默认模型经 OpenAI 官方模型目录核对后保留 `gpt-5.6-luna`；微信隐私配置按官方全局配置规则修正；
- 自动验证：70 项 Node 测试通过；17 个页面、9 个云函数、89 个 JavaScript 文件通过结构与语法检查；主包源码 239.6 KiB；
- 真实微信开发者工具、iOS/Android、屏幕阅读器、模型密钥/费用、双人奖励可用性和专业内容复核继续作为外部待办。
