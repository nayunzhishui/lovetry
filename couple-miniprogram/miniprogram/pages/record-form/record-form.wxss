.form-hero { padding-bottom: 18rpx; }
.form-panel { margin-top: 4rpx; }
.draft-ribbon { display: flex; align-items: center; justify-content: space-between; gap: 18rpx; margin-bottom: 26rpx; padding: 18rpx 20rpx; border-left: 5rpx solid var(--sage); background: var(--surface-muted); }
.draft-ribbon--restored { border-left-color: var(--cobalt); }
.draft-ribbon__title { color: var(--ink-strong); font-size: 23rpx; font-weight: 700; }
.draft-ribbon__desc { margin-top: 4rpx; color: var(--ink-soft); font-size: 19rpx; line-height: 1.45; }
.draft-ribbon button { flex: 0 0 auto; min-height: 58rpx; margin: 0; padding: 0 14rpx; background: transparent; color: var(--cobalt); font-size: 20rpx; line-height: 58rpx; }
.template-section { margin: 4rpx 0 26rpx; }
.template-list { display: grid; gap: 10rpx; margin-top: 12rpx; }
.template-card { min-height: 84rpx; margin: 0; padding: 14rpx 18rpx; border: 1rpx dashed var(--line); background: var(--surface-muted); color: var(--ink-strong); line-height: 1.35; text-align: left; }
.template-card view { font-size: 23rpx; font-weight: 700; }
.template-card text { display: block; margin-top: 4rpx; color: var(--ink-soft); font-size: 19rpx; }
.submit-hint { margin-top: 12rpx; color: var(--ink-soft); font-size: 19rpx; text-align: center; }
.picker-value {
  display: flex;
  align-items: center;
  justify-content: space-between;
  min-height: 84rpx;
  padding: 0 22rpx;
}
.picker-value text { color: var(--ink-soft); font-size: 21rpx; }
.type-fields { margin-top: 28rpx; padding-top: 2rpx; border-top: 1rpx solid var(--line); }
.reflection-note,
.privacy-note {
  margin-top: 26rpx;
  padding: 22rpx 24rpx;
  border-left: 5rpx solid var(--sage);
  border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
  background: #eef2ee;
  color: #56645a;
  font-size: 23rpx;
  line-height: 1.65;
}
.privacy-note { border-left-color: var(--amber); background: #f5eee4; color: var(--amber-deep); }
.time-section { margin-top: 32rpx; padding-top: 28rpx; border-top: 1rpx solid var(--line); }
.section-label { margin-bottom: 14rpx; color: var(--ink-strong); font-size: 26rpx; font-weight: 650; }
.date-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 14rpx; }
.date-grid--end { margin-top: 14rpx; }
.duration-badge {
  display: inline-flex;
  margin-top: 18rpx;
  padding: 10rpx 18rpx;
  border-radius: 24rpx;
  background: #e8eee9;
  color: #536a59;
  font-size: 22rpx;
  font-weight: 650;
}
.visibility-row { display: grid; grid-template-columns: 1fr 1fr; gap: 14rpx; }
.visibility-option {
  min-height: 118rpx;
  margin: 0;
  padding: 20rpx;
  border: 1rpx solid var(--line);
  border-radius: var(--radius-sm);
  background: var(--surface);
  color: var(--ink);
  line-height: 1.4;
  text-align: left;
}
.visibility-option--active { border-color: var(--amber-deep); background: #f5eee4; }
.visibility-title { color: var(--ink-strong); font-size: 25rpx; font-weight: 700; }
.visibility-desc { margin-top: 7rpx; color: var(--ink-soft); font-size: 20rpx; }
.form-error { margin-top: 24rpx; color: var(--danger); font-size: 23rpx; line-height: 1.5; }
.state-card { padding: 80rpx 20rpx; color: var(--ink-soft); text-align: center; }
@media (max-width: 340px) {
  .visibility-row,
  .date-grid { grid-template-columns: 1fr; }
}
