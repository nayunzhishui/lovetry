.home-hero {
  padding-bottom: 24rpx;
}

.sync-ribbon {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin: 6rpx 0 18rpx;
  color: var(--ink-soft);
  font-family: ui-monospace, "SFMono-Regular", Consolas, monospace;
  font-size: 21rpx;
  letter-spacing: 1rpx;
}

.sync-ribbon__light {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
  background: var(--moss);
  box-shadow: 0 0 0 6rpx rgba(115, 142, 120, 0.12);
}

.empty--home {
  padding-top: 46rpx;
  padding-bottom: 34rpx;
  background: rgba(255, 253, 248, 0.62);
}

.empty--home .button {
  margin-top: 30rpx;
}

.space-card {
  position: relative;
  overflow: hidden;
}

.space-card::after {
  position: absolute;
  right: -28rpx;
  bottom: -44rpx;
  width: 130rpx;
  height: 92rpx;
  border: 2rpx solid rgba(113, 133, 119, 0.18);
  border-radius: 50%;
  content: "";
}

.space-code {
  margin-top: 24rpx;
  color: var(--ink-strong);
  font-family: "SFMono-Regular", Consolas, monospace;
  font-size: 44rpx;
  font-weight: 700;
  letter-spacing: 8rpx;
  line-height: 1.25;
  word-break: break-all;
}

.section-head {
  display: flex;
  justify-content: space-between;
  margin-top: 48rpx;
  padding: 0 4rpx;
}

.section-title {
  color: var(--ink-strong);
  font-family: "Songti SC", "STSong", serif;
  font-size: 34rpx;
  font-weight: 700;
}

.moment-panel {
  margin-top: 46rpx;
}
.moment-draft { display: flex; align-items: center; justify-content: space-between; gap: 16rpx; margin-top: 18rpx; padding: 14rpx 18rpx; border-left: 4rpx solid var(--cobalt); background: var(--surface-muted); color: var(--ink-soft); font-size: 21rpx; }
.moment-draft button { min-height: 56rpx; margin: 0; padding: 0 12rpx; background: transparent; color: var(--cobalt); font-size: 20rpx; line-height: 56rpx; }
.moment-draft-hint { margin-top: 12rpx; color: var(--ink-soft); font-size: 19rpx; line-height: 1.5; text-align: center; }

.today-strip {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 10rpx;
  margin-top: 22rpx;
}

.today-metric {
  width: 100%;
  min-height: 116rpx;
  margin: 0;
  padding: 24rpx 12rpx;
  border: 1rpx solid rgba(133, 80, 31, 0.09);
  border-radius: var(--radius-sm);
  background: var(--surface);
  text-align: center;
  line-height: 1.35;
}

.today-metric__value {
  color: var(--ink-strong);
  font-family: ui-monospace, "SFMono-Regular", Consolas, monospace;
  font-size: 32rpx;
  font-weight: 700;
}

.today-metric__label {
  margin-top: 7rpx;
  color: var(--ink-soft);
  font-size: 20rpx;
}

.home-links {
  display: flex;
  justify-content: center;
  gap: 10rpx;
  padding: 30rpx 0 8rpx;
}

.home-links button {
  min-height: 72rpx;
  margin: 0;
  padding: 0 18rpx;
  background: transparent;
  color: var(--amber-deep);
  font-size: 23rpx;
  line-height: 72rpx;
}

.moment-panel textarea {
  min-height: 190rpx;
}
.nav-item--agent { grid-column: 1 / -1; min-height: 142rpx; border-top-color: var(--moss); }
.nav-item--agent .nav-mark { background: var(--moss); }
.relationship-note { display: grid; grid-template-columns: .8fr 1.2fr; gap: 1rpx; margin-top: 14rpx; overflow: hidden; border: 1rpx solid var(--line); border-radius: var(--radius-md); background: var(--line); }
.relationship-note > view { padding: 20rpx; background: var(--surface); }
.relationship-note text { display: block; color: var(--ink-strong); font-family: "Songti SC", "STSong", serif; font-size: 28rpx; font-weight: 700; }
.relationship-note view view { margin-top: 6rpx; color: var(--ink-soft); font-size: 19rpx; }
.week-summary { margin-top: 10rpx; color: var(--ink-soft); font-size: 20rpx; text-align: right; }
.today-actions { display: grid; gap: 8rpx; margin-top: 20rpx; padding: 8rpx; border: 1rpx solid var(--line); border-radius: var(--radius-md); background: var(--surface); }
.today-actions button { display: flex; align-items: center; gap: 16rpx; min-height: 88rpx; margin: 0; padding: 12rpx 14rpx; border-radius: var(--radius-sm); background: transparent; color: var(--ink-strong); line-height: 1.35; text-align: left; }
.today-actions__mark { flex: 0 0 8rpx; width: 8rpx; height: 48rpx; border-radius: 4rpx; background: var(--cobalt); }
.today-actions__mark--us { background: linear-gradient(to bottom, var(--cobalt) 0 45%, var(--coral) 45% 55%, var(--sage) 55%); }
.today-actions__mark--talk { background: var(--coral); }
.today-actions text { display: block; font-size: 23rpx; font-weight: 700; }
.today-actions button view view { margin-top: 3rpx; color: var(--ink-soft); font-size: 19rpx; }
.anniversary-card { display: flex; width: 100%; align-items: center; justify-content: space-between; gap: 24rpx; margin-right: 0; margin-left: 0; text-align: left; line-height: 1.5; }
.anniversary-card__count { min-width: 120rpx; color: var(--coral); text-align: right; }
.anniversary-card__count text { font-family: "Songti SC", STSong, serif; font-size: 58rpx; line-height: 1; }
.anniversary-card__count view { margin-top: 6rpx; color: var(--ink-soft); font-size: 21rpx; }

@media (max-width: 340px) {
  .today-strip { grid-template-columns: 1fr; }
  .today-metric { display: flex; min-height: 80rpx; align-items: center; justify-content: space-between; padding: 16rpx 20rpx; text-align: left; }
  .today-metric__label { margin-top: 0; }
}
