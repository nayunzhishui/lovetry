{
  "name": "lovetry",
  "version": "0.1.0",
  "private": true,
  "description": "情侣微信小程序的本地自动化与共享业务规则",
  "scripts": {
    "sync:shared": "node scripts/sync-runtime-shared.js",
    "test": "node --test tests/*.test.js",
    "eval:agent": "node scripts/evaluate-love-agent.js",
    "verify": "node scripts/verify-project.js",
    "quality": "npm run sync:shared && npm test && npm run eval:agent && npm run verify"
  },
  "engines": {
    "node": ">=22"
  }
}
