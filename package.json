{
  "name": "lovetry",
  "version": "0.1.0",
  "private": true,
  "description": "情侣微信小程序的本地自动化与共享业务规则",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "verify": "node scripts/verify-project.js",
    "quality": "npm test && npm run verify"
  },
  "engines": {
    "node": ">=22"
  }
}
