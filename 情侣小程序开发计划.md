# 情侣小程序开发计划

> 项目路径：`D:\codex\workspace\lovetry`
> 小程序路径：`D:\codex\workspace\lovetry\couple-miniprogram`
> 建立日期：2026-07-15
> 最近更新：2026-07-18
> 状态：P0-P8 本地自动化开发完成；真实环境、开发者工具视觉编译与真机验收冻结
> 唯一执行记录：`项目执行记录.md`

## 1. 项目目标

开发一个仅供一对情侣使用的微信小程序，用于共同记录、规划和回顾生活。核心原则：

- 双人数据同步可靠；
- 情侣空间之间严格隔离；
- 敏感记录默认最小可见；
- 吵架记录帮助复盘，不判断谁对谁错；
- 先完成稳定闭环，再扩展媒体和统计；
- 保持微信原生小程序 + CloudBase 技术路线，不引入独立服务器；
- 每个小任务都包含代码、验证、文档和可交接状态。

### 1.1 MVP 核心闭环

```text
创建情侣空间 → 邀请伴侣加入 → 共同记录/共同计划
→ 日历聚合 → 奖励互动 → 回顾统计 → 数据备份
```

### 1.2 当前事实基准

当前已有：

- 微信原生小程序骨架；
- `login`、`couple`、`records` 三个云函数；
- 首页、吵架复盘、时间线、情侣设置四个页面；
- 情侣空间创建、加入码加入、普通记录、吵架记录和时间线雏形。

当前不足：

- `AppID` 为 `touristappid`，云环境 ID 为占位值；
- 页面直接调用云函数，缺少统一调用模块；
- 缺少统一结果、错误码、加载态和防重复提交；
- 缺少编辑、软删除、可见性、并发版本与双账号验收；
- 加入码使用 `Math.random()`，缺少唯一性、过期和并发加入保护；
- 缺少自动测试、联调页、真实开发者工具和真机验收；
- 原 `项目逐步开发清单.md` 含重复菜单、双重日历、末期隐私审查等问题，不再作为唯一执行入口。

## 2. 需求范围

假设“菜单”指两人共同维护的菜品/餐厅清单及“今天吃什么”，不是应用导航菜单。

| 编号 | 功能 | 第一版范围 | 默认可见性 |
|---|---|---|---|
| F01 | 数据同步 | CloudBase 为事实源，刷新同步，版本号防覆盖 | 按记录权限 |
| F02 | 奖励余额 | 独立钱包、获得/消费/余额/总额、任务奖励和兑换流水 | 情侣双方 |
| F03 | 心情日记 | 心情等级、标题、文字、标签、日期 | 用户选择 |
| F04 | 菜单 | 菜品/餐厅候选、分类、随机选择、吃过标记 | 情侣双方 |
| F05 | 旅行计划 | 日期、目的地、预算、行程、清单、状态 | 情侣双方 |
| F06 | 玩乐记录 | 吃饭、酒店、旅行、约会等统一记录 | 情侣双方 |
| F07 | 生理期记录 | 日期范围、状态、备注；不做医学诊断或预测 | 默认本人 |
| F08 | 吵架记录 | 事实、感受、需要、沟通过程、满意度、下一步约定 | 草稿私密，可共享 |
| F09 | 睡眠记录 | 入睡、醒来、跨天时长、7/30 天统计 | 默认本人 |
| F10 | 游戏记录 | 游戏名、起止时间、时长、共同参与人 | 情侣双方 |
| F11 | 相册 | 私有云存储、相册、图片描述、关联记录 | 仅情侣空间 |
| F12 | 事件/任务 | 负责人、截止日期、清单、状态、可选奖励 | 情侣双方 |
| F13 | 番茄钟 | 专注/休息、中断/完成、关联任务、可选奖励 | 默认本人 |
| F14 | 日历 | 聚合计划、记录、纪念日；隐藏伴侣私密数据 | 动态权限 |

## 3. GitHub 与组件参考

| 项目 | 参考点 | 使用规则 |
|---|---|---|
| `UxxHans/Rainbow-Cats-Personal-WeChat-MiniProgram` | 任务、积分、兑换、伴侣确认 | MIT；参考流程，重写硬编码 OpenID、权限和幂等逻辑 |
| `inannan423/orange-nan-mini_program` | 相册、记事本、纪念日、部署说明 | MIT；可参考页面和上传流程 |
| `rick-ben/our-nest` | 情侣空间、动态、相册、纪念日 | GPL-2.0；默认只参考交互和模型，不直接复制代码 |
| `xuewen-1/love-diary` | 日记、心情、相册、首页结构 | 无明确许可证；只参考产品思路，其手动同步方案不采用 |
| `YPYT1/Account-of-grudges` | 心情日历、问题解决清单 | 无明确许可证；只参考交互 |
| `Tencent/tdesign-miniprogram` | 表单、弹窗、日历、上传和空状态 | MIT；需要时按组件引入，不全量依赖 |
| `wechat-miniprogram/weui-miniprogram` | 微信原生体验组件 | MIT；作为轻量备选，不与 TDesign 混用 |

## 4. 技术方案

### 4.1 总体架构

```text
小程序页面
  ↓
services/cloudApi.js（唯一前端调用接口）
  ↓
login / couple / records / plans / rewards / media / dashboard
  ↓
CloudBase 云数据库 + 云存储
```

不迁移到 uni-app、Taro、Spring Boot 或 SafeHome 的 Flask 后端。复用 SafeHome 的事实基准、执行入口、联调页、验收证据和小步交接方法。

### 4.2 深模块与接口

五个业务模块：

1. 情侣空间模块：身份、情侣空间、加入、成员权限；
2. 共同记录模块：日记、吵架、睡眠、生理期、玩乐、游戏、番茄钟；
3. 共同计划模块：任务、事件、菜单、旅行、纪念日；
4. 奖励模块：钱包、流水、兑换和任务奖励；
5. 媒体模块：相册、图片元数据、访问和删除。

首页、日历和统计属于读取聚合，不拥有另一套业务数据。

### 4.3 云函数

```text
login       保留：获取当前 OpenID
couple      扩展：创建、加入、查询、更新资料、退出
records     扩展：记录 CRUD、筛选、详情、统计
plans       新增：任务、事件、菜单、旅行、纪念日
rewards     新增：钱包、流水、奖励、消费、撤销
media       新增：相册和图片元数据
dashboard   新增：首页、日历和统计聚合
```

统一返回：

```js
{ ok: true, data: {} }
{ ok: false, error: { code: "NO_PERMISSION", message: "无权访问" } }
```

### 4.4 数据集合

```text
couples
memberships
records
plans
wallets
reward_transactions
albums
media_assets
```

记录公共字段：

```js
{
  coupleId,
  type: "moment | mood | conflict | outing | sleep | period | game | pomodoro",
  visibility: "private | couple",
  ownerOpenid,
  title,
  content,
  startAt,
  endAt,
  metrics: {},
  relatedPlanId,
  version,
  createdAt,
  updatedAt,
  deletedAt
}
```

计划公共字段：

```js
{
  coupleId,
  type: "task | event | menu | trip | anniversary",
  title,
  detail,
  status: "todo | doing | done | archived",
  assigneeOpenids: [],
  startAt,
  endAt,
  rewardPoints,
  payload: {},
  version,
  createdBy,
  createdAt,
  updatedAt,
  deletedAt
}
```

奖励流水必须包含 `sourceType + sourceId + idempotencyKey`，任务重复完成不得重复发放奖励。

### 4.5 隐私与安全

- 所有数据库读写经云函数；
- 每次查询、详情、修改、删除都校验情侣成员关系；
- 生理期、睡眠、番茄钟默认私密；
- 心情和吵架记录由创建者选择是否共享；
- 删除采用软删除，图片删除同时清理云存储；
- 加入码使用安全随机数、唯一检查、过期时间、失败次数限制；
- 第二位成员加入使用事务或条件更新，避免第三人并发加入；
- 奖励发放、任务完成、钱包余额和流水写入使用服务端事务；
- 不在日志输出记录正文、生理期内容、图片地址等隐私；
- 支持导出自己的数据和注销/解除情侣关系流程。

### 4.6 页面信息架构

```text
首页：在一起天数、今日任务、最近心情、余额、快捷新增
日历：月历、事件点、类型筛选、日期详情
记录：心情、睡眠、生理期、吵架、玩乐、游戏、番茄钟
我们：相册、旅行、菜单、奖励、情侣设置
```

视觉方向：温暖、私密、克制、非模板化。签名元素为“两个人的双轨时间线”：共享事件显示为两条细线汇合，私密记录只显示本人单轨。建立统一颜色、字体、间距、圆角和阴影变量，避免大面积粉色与无意义装饰。

## 5. 自动化执行规则

- 严格按依赖顺序执行；
- 每次只把一个小任务从未开始推进到完成；
- 业务规则优先采用测试驱动：一个行为测试 → 最小实现 → 通过 → 下一个行为；
- 新页面同步注册 `app.json`；
- 新云函数必须包含 `index.js`、`package.json`；
- 页面必须具备加载、空、失败、防重复提交状态；
- 每完成一个小任务，更新 `项目执行记录.md` 和本文件复选框；
- 不把模拟器通过写成真机通过；人工外部验收必须单独标记；
- 不删除现有可用能力，字段迁移需兼容旧数据；
- 提交前执行 `git status`、静态检查、自动测试和差异检查；
- Git 提交采用 `type(scope): summary`，一个提交对应一个可审查闭环。

## 6. 分阶段任务清单

## P0 工程与安全地基

目标：现有原型变成可继续开发、可测试、可双账号联调的工程。

### P0-01 建立唯一计划和执行记录

- [x] 建立 `情侣小程序开发计划.md`；
- [x] 建立 `项目执行记录.md`；
- [x] 记录当前代码事实和旧计划降级说明；
- [x] 记录技能、验证和提交规则。

验收：新对话只读这两个文件即可知道目标、现状和下一步。

### P0-02 建立自动测试与共享业务模块

- [x] 新增根级 `package.json` 和测试命令；
- [x] 新增共享错误、结果、可见性、记录类型和计划类型常量；
- [x] 抽取可在 Node 环境测试的纯业务模块；
- [x] 为记录校验、睡眠时长、奖励幂等和权限判断建立行为测试；
- [x] 确认测试先失败后通过。

验收：`npm test` 可独立运行且覆盖关键纯业务规则。

### P0-03 统一小程序云调用模块

- [x] 新增 `miniprogram/services/cloudApi.js`；
- [x] 封装统一成功/错误转换；
- [x] 封装 login/couple/records 调用；
- [x] 首页、设置、时间线、吵架页迁移到统一模块；
- [x] 增加保存锁和明确错误提示。

验收：页面文件中不再重复直接调用 `wx.cloud.callFunction`。

### P0-04 强化情侣空间绑定

- [x] 加入码改为安全随机生成；
- [x] 增加唯一性、过期时间和标准化；
- [x] 增加最多两人、`memberships` 唯一归属和并发加入保护；
- [x] 增加更新情侣资料、整空间事务归档接口；
- [x] 保持旧情侣数据可读取并惰性补建 membership。

验收：第三人不能加入；无效、过期、已满错误明确。

### P0-05 升级统一记录模块

- [x] 保留旧 `moment/conflict` 数据兼容；
- [x] 新增详情、更新、软删除、分页、类型筛选；
- [x] 新增 visibility、ownerOpenid、version；
- [x] 所有操作执行成员和记录权限检查；
- [x] 服务端计算时间并在事务中检查版本冲突。

验收：伴侣无法读取 private 记录；旧记录仍显示。

### P0-06 建立联调测试页

- [x] 新增 `pages/integration-test`；
- [x] 展示七个云端模块检查结果；
- [x] 支持创建带 `isTest` 标记记录；
- [x] 显示成功、失败、错误码；
- [x] 在设置工具区保留，不作为用户主功能。

验收：一页可判断基础云链路状态。

### P0-07 建立基础 UI 系统

- [x] 建立颜色、字号、间距、圆角、阴影和动效变量；
- [x] 建立共享 card、button、field、empty、status 样式；
- [x] 重构首页、设置、时间线和吵架页；
- [x] 增加小屏、长文本、安全区和减少动效适配；
- [x] 保持文案非评判、可操作。

验收：四个现有页面视觉和状态一致。

### P0-08 P0 自动验收

- [x] JS 语法检查；
- [x] JSON 解析检查；
- [x] 自动测试通过；
- [x] 页面注册和云函数结构检查；
- [x] `git diff --check` 通过；
- [x] 执行独立代码审查并修复阻断问题。

### P0-09 P0 人工验收

- [ ] 微信开发者工具导入；
- [ ] 配置真实 AppID 和 CloudBase 环境；
- [ ] 上传部署 login/couple/records；
- [ ] 两个微信账号创建/加入/记录同步；
- [ ] 第三个账号越权测试；
- [ ] iOS/Android 真机检查。

说明：需要真实账号和开发者工具的步骤，如当前环境无法完成，必须保留为人工待办，不得伪造完成。

## P1 情侣核心闭环

目标：完成最常用的共同记录、任务、日历和奖励闭环。

### P1-01 首页与功能中心

- [x] 建立四类核心入口和快捷新增面板；
- [x] 首页展示情侣状态、在一起天数、今日任务、最近心情、余额和专注；
- [x] 未绑定、加载、空和失败状态完整；
- [x] 页面按需加载聚合数据。

### P1-02 心情日记

- [x] mood 表单、列表、详情、编辑、删除；
- [x] 心情等级、标签、文字和日期；
- [x] private/couple 可见性选择；
- [x] 日历聚合点位。

### P1-03 吵架复盘升级

- [x] 字段调整为事实、感受、需要、沟通、满意度、约定；
- [x] 默认私密草稿，主动共享；
- [x] 兼容现有 conflict payload；
- [x] 不出现输赢、诊断、贴标签文案。

### P1-04 任务与事件计划

- [x] 新增 plans 云函数和集合；
- [x] task/event CRUD；
- [x] 负责人、日期、状态和清单；
- [x] 完成/重新打开与版本并发控制；
- [x] 可选奖励积分和伴侣确认。

### P1-05 基础日历

- [x] 月历和日期详情；
- [x] 聚合任务、事件、纪念日、心情和吵架记录；
- [x] 类型筛选和结果跳转；
- [x] 私密记录仅本人可见；
- [x] 使用本地月边界、ISO 云端范围和可复核日期逻辑。

### P1-06 奖励账户和流水

- [x] 新增 rewards 云函数、wallets 和 reward_transactions；
- [x] 获得、消费、余额、总获得、总消费；
- [x] 任务奖励和手工请求均使用持久幂等键；
- [x] 消费不能导致负余额；
- [x] 伴侣确认与完整流水已实现；首版不提供财务式撤销，修正通过新流水完成。

### P1-07 P1 验收

- [x] 自动测试和静态检查；
- [ ] 两账号核心闭环；
- [x] 奖励重复请求幂等纯业务测试；
- [ ] 真实 CloudBase 并发奖励测试；
- [x] 私密心情/吵架默认可见性与越权纯业务测试；
- [ ] 开发者工具和真机人工验收。

## P2 共同生活模块

目标：完成菜单、旅行和各类生活记录。

### P2-01 菜单

- [x] 菜品/餐厅候选 CRUD；
- [x] 分类、标签、喜欢程度；
- [x] 随机选择与避免立即重复；
- [x] 标记吃过并生成 outing 记录；
- [x] 空菜单引导。

### P2-02 旅行计划

- [x] trip 计划 CRUD；
- [x] 目的地、日期、预算、状态；
- [x] 行程节点和准备清单；
- [x] 与日历、玩乐记录关联；
- [x] 旅行完成状态。

### P2-03 玩乐记录

- [x] outing 类型支持吃饭、酒店、旅行、约会；
- [x] 时间、地点、金额、评价、备注；
- [x] 按分类和日期筛选；
- [x] 关联旅行或菜单。

### P2-04 睡眠记录与分析

- [x] 入睡、醒来、质量和备注；
- [x] 正确计算跨天时长；
- [x] 7/30 天平均时长和前周期趋势；
- [x] 默认 private，可主动共享；
- [x] 无数据和异常时间提示。

### P2-05 生理期记录

- [x] 开始/结束日期、状态和备注；
- [x] 日期范围校验；
- [x] 默认且始终由本人控制可见性；
- [x] 只做记录，不做医学诊断或确定性预测；
- [x] 日历本人可见。

### P2-06 游戏记录

- [x] 游戏名、起止时间、时长、参与人；
- [x] 最近 7/30 天统计；
- [x] 支持编辑、删除和日期筛选；
- [x] 跨午夜时长测试。

### P2-07 番茄钟

- [x] 专注/休息状态机；
- [x] 开始、暂停、继续、中断、完成；
- [x] 可关联任务；
- [x] 完成后写 pomodoro 记录；
- [x] 待保存记录持久化并以确定性请求 ID 防止重复写入；关联任务奖励仍由伴侣确认一次；
- [x] 页面离开/恢复时间校准。

### P2-08 P2 验收

- [x] 各模块 CRUD、筛选和静态检查；
- [x] 睡眠/游戏跨午夜纯业务测试；
- [x] 生理期默认私密与权限纯业务测试；
- [x] 番茄钟确定性请求 ID 幂等测试；
- [x] 日历聚合静态回归；
- [ ] 开发者工具和真机人工验收。

## P3 相册与媒体

目标：完成情侣私有相册，不泄露图片。

### P3-01 相册数据模块

- [x] 新增 media 云函数；
- [x] albums/media_assets 集合；
- [x] 相册 CRUD、排序和封面；
- [x] 图片元数据支持关联记录或旅行。

### P3-02 图片选择、压缩和上传

- [x] 声明所需隐私能力；
- [x] 限制数量、格式和大小；
- [x] 上传前压缩；
- [x] 云路径包含 coupleId/ownerOpenid/随机名；
- [x] 数据库仅保存 fileID，不保存长期临时 URL。

### P3-03 私有访问和删除

- [ ] 配置云存储私有规则；
- [x] 获取短期访问链接；
- [x] 删除元数据时同步删除文件；
- [x] 失败补偿和孤儿文件记录；
- [ ] 第三账号访问验证。

### P3-04 相册页面

- [x] 相册列表、照片网格、预览；
- [x] 上传进度、失败提示和空状态；
- [x] 元数据关联日记/旅行；
- [x] 大量图片分页。

### P3-05 P3 验收

- [ ] 上传、预览、删除和权限测试；
- [x] fileID、cloudPath 和临时链接代码检查；
- [x] 图片数量/大小限制代码检查；
- [ ] 开发者工具和真机人工验收。

## P4 聚合、备份与发布收口

目标：完成统计、备份、隐私和发布前验收。

### P4-01 Dashboard 聚合

- [x] 首页聚合接口；
- [x] 日历聚合接口；
- [x] 奖励、任务、记录和专注统计；
- [x] 查询分页实现并给出生产索引清单；
- [x] 权限过滤进入数据库查询，不返回无权查看的私密摘要。

### P4-02 搜索与筛选

- [x] 按关键词、类型、日期搜索；
- [x] 统一结果卡片和跳转；
- [x] 空结果和单次 100 项上限；
- [x] 搜索复用服务端私密权限过滤。

### P4-03 数据导出和备份

- [x] 导出当前情侣空间共享数据和本人私密数据；
- [x] 不导出伴侣私密数据；
- [x] JSON 版本号、导出时间和截断标记；
- [x] 备份恢复校验和 `restoredFromId` 重复数据策略；
- [x] 解除关系前导出提醒。

### P4-04 隐私和关系安全收口

- [x] 隐私保护指引；
- [x] 图片、日期和敏感记录用途说明；
- [x] 数据删除、整空间归档和注销说明；
- [x] 吵架记录非评判文案审查；
- [x] 禁止监控、控制和公开排行设计。

### P4-05 性能和可靠性

- [ ] 数据库索引；
- [x] 分页、按需加载和图片按页加载；
- [x] 只读请求超时与一次重试、写请求防自动重放、断网和恢复提示；
- [x] CloudBase 用量和费用告警说明；
- [x] 备份格式、空间归属、截断拒绝和数量上限自动恢复演练；
- [ ] 真实 CloudBase 数据恢复演练。

### P4-06 全链路验收页

- [x] 聚合 login/couple/records/plans/rewards/media/dashboard 状态；
- [x] 测试数据使用 `isTest`；
- [x] 支持清理测试数据；
- [x] 输出明确错误码。

### P4-07 发布前验证

- [x] 全量自动测试；
- [x] JS/JSON/结构/差异检查；
- [ ] 微信开发者工具全页面检查；
- [ ] 两账号和第三账号验收；
- [ ] iOS/Android 真机验收；
- [ ] 体验版连续试用 7 天；
- [ ] 隐私配置和提审材料检查。

### P4-08 最终文档与交接

- [x] 所有本地任务状态与代码一致；
- [x] 更新运行、云环境和部署说明；
- [x] 更新数据库和云函数说明；
- [x] 记录已知限制和外部人工待办；
- [x] 完成独立代码审查并修复 P0/P1 问题；
- [x] 提交所有已验证改动。

## 7. 完成定义

计划完成必须同时满足：

1. 所有可在本地自动完成的小任务完成；
2. 自动测试、静态检查和差异检查通过；
3. 两账号核心链路、第三账号隔离、奖励幂等和私密数据测试有证据；
4. 微信开发者工具、真实 CloudBase、iOS/Android 真机等外部项由用户实际验收；
5. 本文件与 `项目执行记录.md` 状态一致；
6. 不把未执行的外部人工验收标为完成。

## 8. 工期估算

- 有经验开发者全职：约 7～8 周；
- 初学者每天 2～3 小时：约 12～16 周；
- 不含微信审核等待和定制插画制作。

Codex 自动执行不按自然周等待，已完成本轮核心落地与本地可靠性增强；真实 AppID、CloudBase 控制台、双账号、第三账号、微信开发者工具编译和真机操作保留为待办。未勾选项即为尚未完成或未取得真实环境证据，不伪装为完成。

## P5 GitHub 对标增强与 UI 美化

边界：本阶段只执行可在本地编码和自动验证的任务。真实 AppID、CloudBase 资源创建/部署、订阅模板配置、开发者工具编译、真机与三账号验收统一标记为冻结，不阻塞本地开发。

### P5-00 冻结外部环境任务

- [x] 保留 P0-09、P3 云存储规则、P4 数据库索引和 P4-07 为未完成；
- [x] 不写入真实 AppID、环境 ID、订阅模板 ID 或私钥；
- [x] 所有新能力提供占位配置、部署说明和本地自动测试。

### P5-01 GitHub UI 研究与美术系统

- [x] 对比 Rainbow Cats、Orange Nan、Our Nest、Love Diary、TDesign 和 WeUI；
- [x] 建立 `design/context.md`、`design/research.md` 和 `design/directions.md`；
- [x] 选定“Twin Almanac 双人年鉴”方向；
- [x] 更新全局色彩、字体、间距、卡片、表单和状态样式；
- [x] 美化首页、日历、记录、计划、奖励、相册和设置；
- [x] 增加四入口主导航和共享/私密双轨标识；
- [x] 完成小屏、深色系统、减少动效和长文本检查。

### P5-02 增量同步与冲突反馈

- [x] 新增服务端 `syncSince` 增量摘要接口；
- [x] 增加前台恢复、下拉刷新和活动页面定时同步策略；
- [x] 显示最近同步时间、离线状态和远端更新提示；
- [x] 保留版本冲突提示，不让后台同步覆盖本地编辑；
- [x] 增加游标、空增量、失效游标和权限测试。

### P5-03 提醒中心与订阅消息准备

- [x] 新增 `notifications` 云函数和通知偏好/提醒任务模型；
- [x] 支持任务截止、纪念日、奖励确认三类提醒；
- [x] 设置页提供提醒开关与订阅授权入口；
- [x] 模板 ID 缺失时给出可操作提示，不调用无效接口；
- [x] 定时触发、模板 ID 和真实发送保持冻结；
- [x] 增加提醒时间计算、幂等键和隐私测试。

### P5-04 奖励商城、仓库与兑现

- [x] 新增奖励商品和库存模型；
- [x] 支持商品创建、上下架、兑换、确认兑现和使用完成；
- [x] 兑换与扣分使用同一事务和幂等键；
- [x] 奖励页增加账户/商城/仓库三段视图；
- [x] 增加余额不足、重复兑换、越权与状态机测试。

### P5-05 多纪念日与倒计时

- [x] 支持创建多个纪念日、按年重复和提前提醒天数；
- [x] 首页展示最近纪念日倒计时；
- [x] 日历显示当年纪念日实例；
- [x] 纪念日编辑入口复用计划模块；
- [x] 增加闰日、跨年、当天和时区测试。

### P5-06 备份 2.0

- [x] 保持版本 1 读取兼容并新增版本 2 清单；
- [x] 支持写入本地 JSON 文件、微信文件分享和文件选择导入；
- [x] 对备份内容计算摘要并在恢复前显示范围；
- [x] 大数据明确分批导出，不恢复钱包余额和失效云文件；
- [x] 增加版本兼容、摘要、截断和重复恢复测试。

### P5-07 共同动态与轻回应

- [x] 将共享 moment/mood/outing 组合为共同动态流；
- [x] 支持克制的轻回应，不增加公开排行；
- [x] 私密记录不进入动态查询；
- [x] 回应写入使用成员校验和幂等键；
- [x] 增加越权、重复回应和删除记录测试。

### P5-08 组件化与分包

- [x] 抽取状态页、双轨标识、档案卡和确认弹窗组件；
- [x] 新增能力页面进入 `features` 分包；
- [x] 页面统一加载、空、失败和重试文案；
- [x] 不同时引入 TDesign 与 WeUI 全量依赖；
- [x] 增加组件注册、分包路径和页面结构检查。

### P5-09 CI、测试与可观测性

- [x] 新增本地 `verify` 命令，统一测试、语法、JSON 和结构检查；
- [x] 新增 GitHub Actions 验证工作流；
- [x] 增加 `miniprogram-ci` 手动预览工作流模板，私钥只从 Secrets 读取；
- [x] 请求携带追踪 ID，日志只记录 action/code/duration；
- [x] 增加云函数公共返回契约和核心页面行为测试；
- [x] 全量验证、代码审查、更新执行记录并提交。

## P6 一小时全栈质量冲刺（2026-07-16）

边界：继续只完成可由本地代码、自动测试和静态检查证明的任务；不执行真实 CloudBase 部署、开发者工具编译、订阅消息真实发送、双账号/第三账号或真机验收。

### P6-01 数据一致性与幂等安全

- [x] 为提醒偏好建立独立领域模块；
  - [x] 更新开关时保留 `templateIds` 与 `consentedAt`；
  - [x] 登记订阅时保留既有提醒开关；
  - [x] 限制有效订阅模板数量并补充测试。
- [x] 加固记录创建幂等；
  - [x] 对规范化请求生成稳定摘要；
  - [x] 同一请求标识、相同内容返回已有记录；
  - [x] 同一请求标识、不同内容返回 `IDEMPOTENCY_CONFLICT`；
  - [x] 防止已存在的确定性文档被静默覆盖。
- [x] 加固计划快捷操作并发；
  - [x] 状态、清单和删除统一进入数据库事务；
  - [x] 客户端传递版本号；
  - [x] 服务端拒绝过期版本；
  - [x] 返回真实递增后的版本与完整计划。
- [x] 修复情侣资料和邀请码更新后返回旧版本的问题。

### P6-02 增量同步正确性

- [x] 将记录、计划、提醒从单一分页偏移改为独立偏移；
- [x] 分类别返回 `hasMoreByType` 与 `nextOffsets`；
- [x] 客户端按独立偏移递归拉取；
- [x] 合并分页时按文档 ID 去重；
- [x] 增加独立偏移、重复页和游标测试。

### P6-03 搜索体验重构

- [x] 删除面向用户暴露的 `mood`、`trip` 等内部类型码；
- [x] 按“记录/计划”动态提供中文类型选择；
- [x] 增加日期范围校验和清除筛选；
- [x] 搜索结果显示来源、中文类型、发生日期和明确跳转提示；
- [x] 搜索结果使用原生按钮并补充领域测试。

### P6-04 UI、无障碍与信任体验

- [x] 首页快捷入口、纪念日卡片和次级入口改为原生按钮；
- [x] 日历日期、日历事件、记录卡、相册标签和照片操作改为原生按钮；
- [x] 提升筛选器与关键小控件的触控高度和文字尺寸；
- [x] 设置页新增“敏感记录默认私密”说明；
- [x] 生产默认隐藏联调入口，仅允许配置显式开启；
- [x] 保持小屏、深色模式和减少动效兼容。

### P6-05 性能与工程质量门禁

- [x] 番茄钟刷新频率由 500ms 降为 1000ms，仍按绝对时间校准；
- [x] 新增 `npm run quality` 一键质量命令；
- [x] 自动拒绝页面绕过统一服务层调用云函数；
- [x] 自动拒绝普通 `view/text` 承担点击操作；
- [x] 增加主包源码 1 MiB 内部预算；
- [x] 更新 README、云环境说明、计划和执行记录；
- [x] 运行测试、结构验证和 `git diff --check`。

### P6-06 外部环境冻结项

- [ ] 真实 AppID 与 CloudBase 环境配置；
- [ ] 云函数、集合、索引、存储规则和告警部署；
- [ ] 订阅消息模板配置与真实发送；
- [ ] 微信开发者工具全页面视觉检查；
- [ ] 双账号、第三账号、弱网和并发实测；
- [ ] iOS/Android 真机、体验版试用与提审。

## P7 恋爱智能 Agent 与知识库（2026-07-16）

### P7-01 本地恋爱知识库

- [x] 建立 12 条可审查知识条目，覆盖沟通、倾听、冲突修复、边界与同意、信任、异地、独立空间、关系复盘、约会、金钱、道歉和关系安全；
- [x] 定义稳定引用编号、主题、关键词、摘要、说明和可执行步骤；
- [x] 建立中文 n-gram 与关键词混合检索；
- [x] 返回本次实际引用的知识条目；
- [x] 编写知识库扩充和审核规则；
- [ ] 知识库超过约 200 条后再评估向量数据库迁移。

### P7-02 Agent 服务与模型适配

- [x] 新增独立 `love-agent` 云函数；
- [x] 接入 OpenAI Responses API 兼容接口；
- [x] API 密钥仅从云函数环境变量读取；
- [x] 模型、API Base 可配置；
- [x] 请求设置 `store: false`；
- [x] 未配置密钥或模型失败时自动回退本地知识库回答；
- [x] 每个 OpenID 每天最多 50 次真实模型请求；
- [x] 不保存问题正文、回答或会话历史。

### P7-03 可信与安全边界

- [x] 明示 AI 身份和可能出错；
- [x] 默认无记忆，不自动读取情侣记录；
- [x] 禁止监控、欺骗、报复、强迫和操控建议；
- [x] 不进行人格、依恋或心理疾病诊断；
- [x] 暴力、强迫、自伤等情境优先现实安全支持；
- [x] 模型引用只能指向本次检索到的知识条目；
- [x] 不替用户发送消息或执行关系决定。

### P7-04 Agent 交互与 UI

- [x] 新增“恋爱助手”分包页面和首页入口；
- [x] 未建立情侣空间也可使用；
- [x] 提供四个新手建议问题，降低空白输入压力；
- [x] 展示 AI/知识库/安全三种回答模式；
- [x] 展示参考知识条目、复制回答和清空本次会话；
- [x] 对话仅保留在当前页面内；
- [x] 保持 Twin Almanac 视觉、深色模式和触控尺寸。

### P7-05 Agent 自动验证与部署冻结

- [x] 增加检索、安全分级、降级回答、历史限制、响应解析和引用约束测试；
- [x] 自动验证页面、云函数、JSON 和锁文件结构；
- [x] 更新隐私、部署、计划和执行记录；
- [ ] 配置真实 `OPENAI_API_KEY`；
- [ ] 创建 `agent_usage` 集合并部署 `love-agent`；
- [ ] 在真实环境验证模型响应、费用、网络可达性和配额；
- [ ] 对知识内容进行专业人员复核和长期评测。

### P7-06 通用 API 接入与自检（2026-07-17）

- [x] 保持 OpenAI Responses API 默认路径；
- [x] 新增 OpenAI 兼容 Chat Completions 协议；
- [x] 支持通用密钥、API Base、相对路径、模型、超时和输出上限配置；
- [x] Chat Completions 输出令牌字段兼容 `max_tokens` 与 `max_completion_tokens`；
- [x] 默认强制 HTTPS，仅允许显式开启本地 HTTP 调试；
- [x] 增加脱敏配置状态与真实短请求连接测试；
- [x] 区分密钥失败、地址不匹配、限流、超时和服务不可用；
- [x] 模型调用失败时提示用户并自动回退本地知识库；
- [x] 增加双协议请求、响应解析和配置脱敏测试；
- [x] 更新模型配置、部署说明和项目执行记录；
- [ ] 在真实 CloudBase 设置密钥、部署并点击“测试 API”完成验收。

## P8 GitHub 对标录入体验优化（2026-07-18）

### P8-01 草稿恢复深模块

- [x] 建立保存、恢复、清理三个接口的本机草稿模块；
- [x] 草稿按页面和业务类型隔离，避免心情、睡眠、旅行等内容串用；
- [x] 草稿 7 天后自动失效，存储异常不阻断表单；
- [x] 正式保存成功后立即清除对应草稿；
- [x] 页面明确显示草稿状态并允许移除备份；
- [x] 增加恢复、过期和模板行为测试。

### P8-02 记录与计划快速模板

- [x] 为生活、心情、沟通、玩乐、睡眠、生理期和游戏提供结构化模板；
- [x] 为任务、事件、菜单、旅行和纪念日提供快速模板；
- [x] 模板只提供可编辑框架，不生成虚假经历；
- [x] 切换类型前保存当前草稿，新类型独立恢复；
- [x] 编辑已有记录同样支持本机草稿恢复。

### P8-03 日历与奖励捷径

- [x] 日历突出今天并提供“回到今天”；
- [x] 空日期直接创建当天记录或事件；
- [x] 从日历进入表单时预填选中日期和类型；
- [x] 奖励获得/消费提供 5、10、20、50 快捷档位；
- [x] 首页快速生活记录增加自动草稿恢复。

### P8-04 自动验证与外部待办

- [x] 更新 GitHub 研究、计划和执行记录；
- [x] 运行单元测试、页面结构、语法、包体和交互语义检查；
- [ ] 微信开发者工具检查草稿提示、长文本、小屏和深色模式；
- [ ] iOS/Android 真机验证退出恢复、清缓存和弱网保存体验。
